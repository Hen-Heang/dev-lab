# Architecture Notes

Notes on *why* AuthHub is structured the way it is — decisions, tradeoffs, things that were
tried and reverted. The current-state module map lives in `docs/architecture.md`; this folder
is for the reasoning and history behind it, not a duplicate of it.
