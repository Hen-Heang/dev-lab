# Common Patterns

Recurring code patterns worth copying rather than re-deriving each time — MapStruct mapper
setups, exception translation in `GlobalExceptionHandler`, service/impl split conventions, etc.
