server:
  port: ${PORT:8080}

spring:
  datasource:
     url: ${DATABASE_URL}
     username: ${DATABASE_USERNAME}
     password: ${}

jpa:
  hibernate:
    ddl-auto: update
    show-sql: false

logging:
  level:
    root: INFO
    com.henheang.heangapicenter: INFO